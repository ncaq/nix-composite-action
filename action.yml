name: Nix Composite
author: ncaq
description: Install Nix and configure caches (Cachix, niks3)

branding:
  icon: "package"
  color: "purple"

inputs:
  extra-nix-config:
    description: Append nix.conf settings
    required: false
    default: ""
  use-daemon:
    description: >-
      Use nix-daemon post-build-hook to push store paths.
      Set to false on self-hosted runners where the daemon cannot reach the hook path
      (e.g. containerized runners without host bind-mount configuration).
    required: false
    default: "true"
  cachix-name:
    description: Cachix cache name
    required: false
    default: ncaq
  cachix-auth-token:
    description: Cachix authentication token
    required: false
    default: ""
  niks3-endpoint:
    description: niks3 server endpoint URL
    required: false
    default: "https://niks3-public.ncaq.net"

runs:
  using: composite
  steps:
    - uses: cachix/install-nix-action@8aa03977d8d733052d78f4e008a241fd1dbf36b3 # v31.10.6
      with:
        # セルフホストランナーでうっかり外部からの悪意あるPRを実行許可してしまい、
        # そのまま`flake.nix`の設定を受け入れるとリスクが比較的高いため、
        # リスク軽減のためにGitHubホステッドランナーでのみ、
        # `flake.nix`の設定を自動で受け入れるようにしています。
        # GitHubホステッドランナーは荒らされても比較的安全なので受け入れるようにしています。
        # GitHubホステッドランナーでは`runner.environment`は`github-hosted`になります。
        # 仮に値が`github-hosted`にならなくてもキャッシュリストが読み込まれないだけなので、
        # リスクはありません。
        extra_nix_config: |
          ${{ runner.environment == 'github-hosted' && 'accept-flake-config = true' || '' }}
          ${{ inputs.extra-nix-config }}

    - name: Setup Cachix cache
      if: inputs.cachix-name != '' && inputs.cachix-auth-token != ''
      uses: cachix/cachix-action@5f2d7c5294214f71b873db4b969586b980625e71 # v17
      with:
        name: "${{ inputs.cachix-name }}"
        authToken: "${{ inputs.cachix-auth-token }}"
        # フォークからのPRはキャッシュへのプッシュをスキップ。
        skipPush: ${{ github.event_name == 'pull_request' &&
          github.event.pull_request.head.repo.full_name != github.repository }}
        # デーモンモードの方が効率が良いためデフォルトで有効にします。
        # セルフホステッドランナーでも、
        # ホスト側でpost-build-hookのパスを到達可能にすれば動作します。
        # ただしランナーホストの設定を変更できない特殊なCI環境では、
        # `use-daemon: false`でstore scanモードに切り替えてください。
        useDaemon: "${{ inputs.use-daemon }}"

    # OIDC認証を使うのでsecretは不要です。
    # セルフホステッドランナーで`trusted-users`に含まれない場合は、
    # `post-build-hook`ではなく`/nix/store`のスキャンによるpushに自動でフォールバックします。
    # `use-daemon`入力でのデーモンモード明示制御は、
    # `Mic92/niks3-action`に対応するinputが追加されたら反映する予定です。
    - name: Setup niks3 cache
      uses: Mic92/niks3-action@74d07d8646174756fd49c870b1c26998f4521f81 # v1.0.1
      with:
        server-url: "${{ inputs.niks3-endpoint }}"
        # フォークからのPRはキャッシュへのプッシュをスキップ。
        skip-push: ${{ github.event_name == 'pull_request' &&
          github.event.pull_request.head.repo.full_name != github.repository }}
